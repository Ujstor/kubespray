---
- name: Set kubeadm_discovery_address
  set_fact:
    # noqa: jinja[spacing]
    kubeadm_discovery_address: >-
      {%- if "127.0.0.1" in kube_apiserver_endpoint or "localhost" in kube_apiserver_endpoint -%}
      {{ first_kube_control_plane_address | ansible.utils.ipwrap }}:{{ kube_apiserver_port }}
      {%- else -%}
      {{ kube_apiserver_endpoint | regex_replace('https://', '') }}
      {%- endif %}
  tags:
    - facts

- name: Obtain kubeadm certificate key for joining control planes nodes
  when:
    - not kube_external_ca_mode
  run_once: true
  block:
    - name: Upload certificates so they are fresh and not expired
      command: >-
        {{ bin_dir }}/kubeadm init phase
        --config {{ kube_config_dir }}/kubeadm-config.yaml
        upload-certs
        --upload-certs
      register: kubeadm_upload_cert
      delegate_to: "{{ first_kube_control_plane }}"

    - name: Parse certificate key if not set
      set_fact:
        kubeadm_certificate_key: "{{ kubeadm_upload_cert.stdout_lines[-1] | trim }}"

- name: Wait for k8s apiserver
  wait_for:
    host: "{{ kubeadm_discovery_address | regex_replace('\\]?:\\d+$', '') | regex_replace('^\\[', '') }}"
    port: "{{ kubeadm_discovery_address.split(':')[-1] }}"
    timeout: 180


- name: Check already run
  debug:
    msg: "{{ kubeadm_already_run.stat.exists }}"

- name: Reset cert directory
  shell: >-
    if [ -f /etc/kubernetes/manifests/kube-apiserver.yaml ]; then
    {{ bin_dir }}/kubeadm reset -f --cert-dir {{ kube_cert_dir }};
    fi
  environment:
    PATH: "{{ bin_dir }}:{{ ansible_env.PATH }}"
  when:
    - inventory_hostname != first_kube_control_plane
    - kubeadm_already_run is not defined or not kubeadm_already_run.stat.exists
    - not kube_external_ca_mode

- name: Get kubeconfig for join discovery process
  command: "{{ kubectl }} -n kube-public get cm cluster-info -o jsonpath='{.data.kubeconfig}'"
  register: kubeconfig_file_discovery
  run_once: true
  delegate_to: "{{ groups['kube_control_plane'] | first }}"
  when:
    - kubeadm_use_file_discovery
    - kubeadm_already_run is not defined or not kubeadm_already_run.stat.exists

- name: Copy discovery kubeconfig
  copy:
    dest: "{{ kube_config_dir }}/cluster-info-discovery-kubeconfig.yaml"
    content: "{{ kubeconfig_file_discovery.stdout }}"
    owner: "root"
    mode: "0644"
  when:
    - inventory_hostname != first_kube_control_plane
    - kubeadm_use_file_discovery
    - kubeadm_already_run is not defined or not kubeadm_already_run.stat.exists

- name: Create kubeadm ControlPlane config
  template:
    src: "kubeadm-controlplane.yaml.j2"
    dest: "{{ kube_config_dir }}/kubeadm-controlplane.yaml"
    mode: "0640"
    backup: true
    validate: "{{ kubeadm_config_validate_enabled | ternary(bin_dir + '/kubeadm config validate --config %s', omit) }}"
  when:
    - inventory_hostname != first_kube_control_plane
    - not kubeadm_already_run.stat.exists

- name: Detect kubelet has already TLS-bootstrapped (CP-join idempotency)
  # If /etc/kubernetes/kubelet.conf exists at this point, kubelet has already
  # bootstrapped against the cluster (typically via the bootstrap_kubelet_conf
  # pre-stage in roles/kubernetes/node) and the Node is registered. kubeadm's
  # kubelet-start phase would then refuse with "Node already exists", and
  # preflight would fail on existing files/ports. Auto-extend skip-phases and
  # ignore-preflight-errors to walk past those.
  stat:
    path: "{{ kube_config_dir }}/kubelet.conf"
  register: _cp_join_kubelet_conf
  when:
    - inventory_hostname != first_kube_control_plane
    - kubeadm_already_run is not defined or not kubeadm_already_run.stat.exists

- name: Compute effective kubeadm join overrides for fresh CP join
  set_fact:
    _cp_join_skip_phases: >-
      {{ kubeadm_join_phases_skip
         + (['kubelet-start'] if (_cp_join_kubelet_conf.stat.exists | default(false))
                                  and 'kubelet-start' not in kubeadm_join_phases_skip
            else []) }}
    _cp_join_ignore_preflight: >-
      {{ kubeadm_ignore_preflight_errors
         + ((_cp_join_extra_ignore_preflight | default([]))
            if (_cp_join_kubelet_conf.stat.exists | default(false)) else []) }}
  vars:
    _cp_join_extra_ignore_preflight:
      - FileAvailable--etc-kubernetes-manifests-kube-apiserver.yaml
      - FileAvailable--etc-kubernetes-manifests-kube-controller-manager.yaml
      - FileAvailable--etc-kubernetes-manifests-kube-scheduler.yaml
      - FileAvailable--etc-kubernetes-manifests-etcd.yaml
      - FileAvailable--etc-kubernetes-kubelet.conf
      - FileAvailable--etc-kubernetes-bootstrap-kubelet.conf
      - DirAvailable--var-lib-etcd
      - Port-6443
      - Port-10250
      - Port-10257
      - Port-10259
      - Port-2379
      - Port-2380
  when:
    - inventory_hostname != first_kube_control_plane
    - kubeadm_already_run is not defined or not kubeadm_already_run.stat.exists

- name: Joining control plane node to the cluster.
  command: >-
    {{ bin_dir }}/kubeadm join
    --config {{ kube_config_dir }}/kubeadm-controlplane.yaml
    --ignore-preflight-errors={{ _cp_join_ignore_preflight | join(',') }}
    --skip-phases={{ _cp_join_skip_phases | join(',') }}
  environment:
    PATH: "{{ bin_dir }}:{{ ansible_env.PATH }}"
  register: kubeadm_join_control_plane
  retries: 3
  throttle: 1
  until: kubeadm_join_control_plane is succeeded
  when:
    - inventory_hostname != first_kube_control_plane
    - kubeadm_already_run is not defined or not kubeadm_already_run.stat.exists

# When we auto-skip kubelet-start (because kubelet had already bootstrapped),
# kubeadm doesn't write /var/lib/kubelet/config.yaml. kubespray's
# `kubeadm_already_run` heuristic stats only that file; without it, the next
# pipeline run treats this CP as un-joined and fires `kubeadm reset`. Stamp
# it from the freshly-rendered KubeletConfiguration so subsequent runs are
# correct. Idempotent (force: false).
- name: Stamp /var/lib/kubelet/config.yaml after CP join (kubelet-start skipped)
  copy:
    src: "{{ kube_config_dir }}/kubelet-config.yaml"
    dest: /var/lib/kubelet/config.yaml
    remote_src: true
    mode: "0644"
    owner: root
    group: root
    force: false
  when:
    - inventory_hostname != first_kube_control_plane
    - _cp_join_kubelet_conf.stat.exists | default(false)
    - 'kubelet-start' in (_cp_join_skip_phases | default([]))
    - kubeadm_join_control_plane is succeeded

- name: Wait for new control plane nodes to be Ready
  when: kubeadm_already_run.stat.exists
  run_once: true
  command: >
    {{ kubectl }} get nodes --selector node-role.kubernetes.io/control-plane
    -o jsonpath-as-json="{.items[*].status.conditions[?(@.type == 'Ready')]}"
  register: control_plane_node_ready_conditions
  retries: "{{ control_plane_node_become_ready_tries }}"
  delay: 5
  delegate_to: "{{ groups['kube_control_plane'][0] }}"
  until: >
    control_plane_node_ready_conditions.stdout
    | from_json | selectattr('status', '==', 'True')
    | length == (groups['kube_control_plane'] | length)
